# Block PHP execution in uploads — Apache 2.4 syntax
Options -Indexes -ExecCGI

<FilesMatch "\.(php|php5|phtml|phar|pl|cgi|py)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
</FilesMatch>
